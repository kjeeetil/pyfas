# Web Chat Example

This example demonstrates a simple web chat interface using the OpenAI API with
persistent sessions and streaming responses.

## Setup

```bash
cd webchat
npm install
# Set your OpenAI key
export OPENAI_API_KEY="your_key"
npm start
```

Open `http://localhost:3000` in your browser and chat away.
